This is example of using explicit receiver parameter that does not work with Spring Validation.

`Foo.validatedFoo` method should throw @ConstraintViolationException when executed on bean in invalid state
